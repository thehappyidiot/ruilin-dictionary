# Database (docker-compose defaults)
POSTGRES_DB="ruilin_dictionary"
POSTGRES_USER="ruilin_user"
POSTGRES_PASSWORD="change-me"

# Database URL used by the app container
DB_URL="postgres://ruilin_user:change-me@db:5432/ruilin_dictionary?sslmode=disable"

# Optional migration settings
GOOSE_DRIVER="postgres"
GOOSE_MIGRATION_DIR="./sql/schema"

# Server config
PORT=8080
HOST_PORT=18080
IS_DEVELOPMENT=false
ADMIN_PASSWORD_HASH="$2a$12$replace-with-bcrypt-hash"
SESSION_SECRET="replace-with-at-least-32-random-characters"
# Optional (default 28800 = 8 hours)
SESSION_MAX_AGE_SECONDS=28800